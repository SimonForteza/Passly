export function LoginPage() {
  return (
    <section>
      <h2>Ingresar</h2>
      <p style={{ color: 'var(--color-texto-secundario)' }}>
        Próximamente: formulario de login (otra issue de frontend).
      </p>
    </section>
  );
}
